# SHINI v4.0.1 release verification

This marker exists only to trigger the clean-room pull-request QA against the exact production source after the `shini-auth` compatibility update and service-worker cache rotation.
